//282. Expression Add Operators
//题意：给一个string包含0~9，尝试添加操作符"+,-,*"使得表达式的值等于目标target，给出所有的可能的表达式的形势

class Solution {
public:
    vector<string> addOperators(string num, int target) {
        vector<int> nums;
        istringstream in(num);
        int a;
        while(in >> a) nums.push_back(a);
    }

    string DandC(vector<int>&nums,int l,int r)
    {

    }
};